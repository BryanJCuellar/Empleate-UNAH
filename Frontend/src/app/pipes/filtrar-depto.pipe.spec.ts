import { FiltrarDeptoPipe } from './filtrar-depto.pipe';

describe('FiltrarDeptoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarDeptoPipe();
    expect(pipe).toBeTruthy();
  });
});
