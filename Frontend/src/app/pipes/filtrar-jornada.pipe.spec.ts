import { FiltrarJornadaPipe } from './filtrar-jornada.pipe';

describe('FiltrarJornadaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarJornadaPipe();
    expect(pipe).toBeTruthy();
  });
});
